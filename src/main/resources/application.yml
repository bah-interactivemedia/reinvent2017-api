#Beanstalk exposes port 5000 by default
server:
  port: 5000

logging:
    level:
      com.sbd.reinvent:DEBUG

amazon:
  dynamodb:
    endpoint: http://localhost:8000/
  aws:
    accesskey: "accesskey"
    secretkey: "secretkey"
